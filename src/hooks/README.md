# `src/hooks`

Client-side React hooks live here. Keep hooks focused on UI state and browser
behavior. Server data access should remain in server components, services, or
server actions when those are introduced.
