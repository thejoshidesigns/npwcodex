# `src/styles`

Global styling currently lives in `src/app/globals.css` because this project
uses the Next.js App Router with Tailwind CSS v4.

Add design tokens, documentation, or shared CSS modules here only when they
outgrow the global stylesheet.
